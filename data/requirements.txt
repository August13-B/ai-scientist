# 锁定精确版本，引入新库时同步更新
pdfplumber==0.11.4
pymupdf==1.28.0
grobid-client-python==0.1.2
numpy==1.26.4
pandas==2.2.2

# ===== RAG 灌库相关 =====
# Chroma 1.0 使用 scripts/rag_common.py 的 v2 HTTP 客户端，避免 Windows/Python 3.12
# 环境编译 chroma-hnswlib；因此灌库端无需安装 chromadb SDK。
pymilvus==2.4.9
dashscope==1.20.0
python-dotenv==1.0.1
